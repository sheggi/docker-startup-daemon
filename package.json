{
  "name": "docker-startup-daemon",
  "version": "1.0.0",
  "description": "listen for same port as provided by docker container. if container not running, start it automatically",
  "main": "index.mjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "dependencies": {
    "dotenv": "^16.0.3"
  }
}
